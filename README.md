# Консольная утилита для просмотра списка участников команды GitHub

Пример использвания:

`bash getTeamMembers.sh -o <organization-name> -n <group-name> -t <access-token>`

`bash getTeamMembers.sh -i <group-id> -t <access-token>`
